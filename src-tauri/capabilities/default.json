{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the UManager application window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
